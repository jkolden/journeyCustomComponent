{
  "name": "journeyCC",
  "version": "1.0.1",
  "description": "Oracle Digital Assistant Component Package",
  "main": "main.js",
  "scripts": {
    "bots-node-sdk": "bots-node-sdk",
    "help": "npm run bots-node-sdk -- --help",
    "prepack": "npm run bots-node-sdk -- pack --dry-run",
    "start": "npm run bots-node-sdk -- service .",
    "docker-build": "docker-compose build --force-rm --no-cache",
    "format": "prettier \"components/**/*.{js,html}\" --write",
    "lint": "eslint \"components/**/*.{js,jsx}\" --quiet"
  },
  "repository": {},
  "dependencies": {
    "axios": "^0.24.0",
    "eslint": "^8.3.0",
    "lint": "^0.7.0",
    "prettier": "^2.5.1",
    "request": "^2.88.2"
  },
  "devDependencies": {
    "@oracle/bots-node-sdk": "^2.5.5",
    "express": "^4.17.1"
  },
  "bundledDependencies": [
    "axios",
    "eslint",
    "lint",
    "prettier",
    "request"
  ]
}